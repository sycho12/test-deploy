---
version: 2
jobs:
  build:
    docker:
      - image: circleci/python:latest
    working_directory: ~/work/repo
    steps:
      - checkout
      - run: pip install -r ansible-lint
      - run: ansible-lint ./*.yml
